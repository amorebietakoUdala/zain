{% if is_granted('ROLE_USER') %}
<nav class="navbar navbar-expand navbar-light bg-light mb-2">
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path("languageHome",{'_locale': app.request.locale })}}">{{ 'menu.home' | trans }}<span class="sr-only">(current)</span></a>
      </li>
{% if is_granted('ROLE_ADMIN') %}
    <li class="nav-item">
	<a class="nav-link" href="{{ path("admin_mevent_list")}}">{{ "menu.mevents" | trans | capitalize }}</a>
    </li>
    <li class="nav-item">
	<a class="nav-link" href="{{ path("admin_event_list")}}">{{ "menu.events" | trans | capitalize }}</a>
    </li>
{% endif %}
 </div>
</nav>
{% endif %}